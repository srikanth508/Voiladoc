import { TranslateLogHandler } from "./TranslateLogHandler";
import { Translator } from "./Translator";
import { TranslatorContainer } from "./TranslatorContainer";
import { PipeTransform } from "@angular/core";
export declare class TranslatePipe implements PipeTransform {
    private translator;
    private translatorContainer;
    private logHandler;
    static pipeName: string;
    private static _parseParams;
    private promise;
    private translation;
    private translated;
    private subscription;
    constructor(translator: Translator, translatorContainer: TranslatorContainer, logHandler: TranslateLogHandler);
    /**
     * Translates key with given args.
     *
     * @see TranslateService.translator
     * @param {string} key
     * @param {object} params
     * @param {string?} module
     * @returns {string}
     */
    transform(key: string, params?: object, module?: string): string;
    module: string;
    private startTranslation;
}
