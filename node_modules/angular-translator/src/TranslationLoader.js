var TranslationLoader = /** @class */ (function () {
    function TranslationLoader() {
    }
    TranslationLoader.prototype.flattenTranslations = function (translations, data, prefix) {
        if (prefix === void 0) { prefix = ""; }
        for (var key in data) {
            if (Array.isArray(data[key])) {
                translations[prefix + key] = data[key].filter(function (v) { return typeof v === "string"; }).join("");
            }
            else if (typeof data[key] === "object") {
                this.flattenTranslations(translations, data[key], prefix + key + ".");
            }
            else if (typeof data[key] === "string") {
                translations[prefix + key] = data[key];
            }
        }
    };
    return TranslationLoader;
}());
export { TranslationLoader };
//# sourceMappingURL=TranslationLoader.js.map