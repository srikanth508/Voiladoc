import { TranslateLogHandler } from "./TranslateLogHandler";
import { TranslatorConfig } from "./TranslatorConfig";
import { Injector } from "@angular/core";
import { Observable } from "rxjs";
export declare class TranslatorContainer {
    private config;
    private logHandler;
    private injector;
    private _language;
    private languageChangedObservable;
    private languageChangedObserver;
    private translators;
    constructor(config: TranslatorConfig, logHandler: TranslateLogHandler, injector: Injector);
    readonly languageChanged: Observable<string>;
    language: string;
    getTranslator(module: string): any;
    /**
     * Detects the preferred language.
     *
     * Returns false if the user prefers a language that is not provided or
     * true.
     *
     * @returns {boolean}
     */
    private detectLanguage;
}
